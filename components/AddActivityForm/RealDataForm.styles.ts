import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: '100%',
  },
  helperText: {
    fontSize: 13,
    lineHeight: 18,
    color: '#64748B',
  },
  activeDateLabel: {
    marginTop: 6,
    fontSize: 16,
    fontWeight: '600',
    color: '#0F172A',
    textTransform: 'capitalize',
  },
});
